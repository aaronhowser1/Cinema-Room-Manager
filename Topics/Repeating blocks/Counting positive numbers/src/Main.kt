fun main() {
    // put your code here
    var positiveCount = 0
    repeat(readln().toInt()) {
        if (readln().toInt()>0) positiveCount++
    }
    println(positiveCount)
    val thing = mutableListOf<Int>(1)
}